# .claude/skills

Purpose:
- Hold optional Claude Code project skills (for example, frontend design helpers).

Intended behavior:
- Let the team add reusable assistant workflows without changing core source code.
