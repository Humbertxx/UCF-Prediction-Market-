# CLAUDE.md

This file is the Claude Code context entrypoint for the Bloomknights project.

## Purpose

- Provide assistant-specific orientation without duplicating core project docs.
- Point to `README.md`, `DESIGN.md`, and `.cursor/rules/` for canonical guidance.
- Keep Claude behavior aligned with repository architecture and constraints.

## Intended use

When Claude Code loads this repository, it should treat this document as an index into the project's primary sources of truth, not as a second architecture spec.
