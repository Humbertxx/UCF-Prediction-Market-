"""Market service layer.

Purpose:
- Handle market lifecycle operations (create, list, fetch, close, resolve).
- Coordinate market persistence with validation and business rules.

Intended behavior:
- Expose clean service methods for route handlers without HTTP coupling.
"""

