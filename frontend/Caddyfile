# SPA static server for the Vite build output (Railway frontend service).
# Railway terminates TLS; keep auto_https off.

{
	auto_https off
}

:{$PORT} {
	root * dist
	encode gzip
	file_server
	try_files {path} /index.html
}
